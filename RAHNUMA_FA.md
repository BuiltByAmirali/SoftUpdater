# راهنمای فارسی SoftUpdater

## تغییرات این نسخه نسبت به نسخه قبلی

1. **دکمه Settings اضافه شد** — اندازه فونت کل برنامه را می‌توانید بزرگ/کوچک کنید
   (۹ تا ۱۸ پوینت). تغییر فوراً روی همه‌ی برنامه اعمال و برای دفعات بعد ذخیره می‌شود.
2. **«Managed by vendor»** — برای برنامه‌هایی که خودشان آپدیت می‌شوند (مثل Steam،
   درایورها، ابزارهای ASUS/Realtek) نمایش داده می‌شود؛ یعنی آپدیت آن‌ها با خودِ سازنده است
   و لازم نیست این برنامه کاری کند. ماوس را روی آن نگه دارید تا توضیح کامل را ببینید.
3. **WinRAR حالا واقعاً بررسی می‌شود** — از صفحه رسمی rarlab.com (نسخه ۷٫۲۳ یا جدیدتر).
4. **Telegram / Qtum / ViGEm برطرف شد** — نام فایل‌های رسمی این برنامه‌ها تغییر کرده بود
   و برنامه نصاب جدید را پیدا نمی‌کرد؛ حالا دوباره کار می‌کند.
5. **ردیف‌های اشتباه حذف شدند** — دیگر برنامه‌هایی که اسمشان با مخزن‌های گیت‌هاب بی‌ربط
   یکسان بود (مثل SpotPlayer) آپدیت اشتباه نشان نمی‌دهند؛ فقط مخزن‌های معتبر
   (سازمان رسمی یا بیش از ۱۰۰ ستاره) به‌عنوان منبع خودکار پذیرفته می‌شوند.
6. **اگر نسخه نصب‌شده با جدیدترین نسخه برابر باشد**، حالا «Up to date» نشان داده می‌شود
   حتی اگر آن انتشار فایل نصابی نداشته باشد.
7. **ردیف‌های «Update for (KB...)» با فاصله‌ی دوتایی هم دیگر لیست نمی‌شوند.**
8. **رابط کاربری کاملاً انگلیسی است** — هیچ متن فارسی در برنامه نیست و کل برنامه LTR است.
9. **عرض ستون‌ها حالا متناسب است** — هیچ ستونی کل فضا را نمی‌گیرد؛ هر ستون سهم خودش را
   دارد و با بزرگ/کوچک کردن پنجره دوباره به‌طور خودکار تنظیم می‌شود.
10. **گزینه «Open installers after download»** — وقتی فعال باشد (پیش‌فرض: فعال)،
    بلافاصله بعد از تمام شدن دانلود هر برنامه، پنجره‌ی نصب‌کننده‌ی آن خودکار باز می‌شود
    تا همان‌جا مراحل آپدیت را جلو ببرید؛ فایل‌ها همچنان در پوشه‌ی Downloads ذخیره می‌شوند.
    اگر بخواهید فقط فایل ذخیره شود، می‌توانید تیک آن را بردارید (انتخاب شما برای
    دفعات بعد هم به خاطر سپرده می‌شود). نصب همیشه با تأیید خودتان (و پنجره UAC
    خود ویندوز) انجام می‌شود.
11. **حجم دقیق آپدیت** — برای برنامه‌هایی که آپدیت دارند، حجم دانلود کنار شماره نسخه
    جدید در ستون Latest نمایش داده می‌شود (مثلاً «7.2.8 · 74.8 MB»). حجم از خود
    منبع رسمی خوانده می‌شود؛ اگر منبع حجم را ندهد، همان نسخه بدون عدد نشان داده می‌شود.
12. **پنل دانلود با توقف/ادامه و سرعت** — موقع دانلود، یک کارت دانلود باز می‌شود که
    درصد پیشرفت، حجم دانلودشده از کل، و سرعت لحظه‌ای را نشان می‌دهد و دکمه‌های
    Pause/Resume و Cancel دارد. توقف/ادامه بدون از دست رفتن داده انجام می‌شود
    (ادامه با HTTP Range)؛ اگر سرور ادامه‌ی رنج را پشتیبانی نکند، دانلود از ابتدا
    شروع می‌شود. این پنل فقط در حال دانلود دیده می‌شود و چیزی را نمی‌پوشاند.
13. **رفع کرش پایان چک کردن + رنگ سبز حجم** — باگی که موقع تمام‌شدن Check for updates
    باعث بسته‌شدن ناگهانی برنامه می‌شد کاملاً برطرف شد (توضیح فنی: نخِ پس‌زمینه
    زودتر از وقت آزاد می‌شد و Qt کل برنامه را قطع می‌کرد؛ حالا برنامه منتظر
    پایان کامل نخ می‌ماند). همچنین عدد حجم فایل در ستون Latest حالا به رنگ
    سبز نمایش داده می‌شود تا از شماره نسخه به‌راحتی قابل تشخیص باشد.
14. **بررسی مجدد فقط یک برنامه (بعد از نصب آپدیت)** — وقتی آپدیت یک برنامه را
    دانلود کردید و نصبش کردید، لازم نیست کل لیست را دوباره چک کنید. از نسخه‌ی
    ۳۱ به بعد این کار **خودکار** انجام می‌شود (بند ۳۱ را ببینید)؛ ولی اگر
    خواستید خودتان هم می‌توانید: روی ردیف
    همان برنامه **کلیک راست** کنید و «Re-check this app» را بزنید. برنامه اول
    یک اسکن سریع از رجیستری انجام می‌دهد تا **نسخه واقعی نصب‌شده‌ی جدید** را
    پیدا کند (فقط همان برنامه، دقی و بدون خطا — اگر نام برنامه در رجیستری
    پیدا نشود، دست به نسخه قبلی نمی‌زند)، بعد فقط همان یک برنامه را با منبع
    رسمی می‌سنجد و ردیفش «Up to date» می‌شود. دکمه «Check selected» هم همین
    اسکن را برای برنامه‌های انتخاب‌شده انجام می‌دهد. موقع بررسی،
    وضعیت ردیف‌ها «Checking...» نمایش داده می‌شود.
15. **هایلایت ردیف انتخاب‌شده** — وقتی روی یک برنامه کلیک می‌کنید، کل ردیف با
    یک نوار شیشه‌ای سفید (مات و ملایم، مثل بلور) روشن می‌شود و یک خط باریک
    سفید هم لبه‌ی چپ ردیف روشن می‌ماند تا انتخاب کاملاً واضح باشد. نگه داشتن
    ماوس روی ردیف‌ها هم یک هایلایت خیلی ملایم نشان می‌دهد. منوی کلیک‌راست هم
    مانند بقیه برنامه، تیره و شیشه‌ای است.
16. **تنظیمات جدید (فقط چیزهای کاربردی)** — بخش Settings حالا چهار بخش کوچک
    دارد: (۱) **Checking**: گزینه «Check for updates when the app starts» —
    اگر فعالش کنید، باز کردن برنامه خودش شروع به چک کردن می‌کند (پیش‌فرض: خاموش).
    (۲) **Downloads**: پوشه دلخواه برای ذخیره نصاب‌ها با دکمه Browse و امکان
    برگشت به پوشه پیش‌فرض. (۳) **Applications**: تعداد برنامه‌های پنهان‌شده و
    دکمه «Restore hidden apps» برای برگرداندن آن‌ها (پنهان کردن از منوی کلیک‌راست
    هر ردیف انجام می‌شود). (۴) **Appearance**: همان اندازه فونت قبلی با پیش‌نمایش
    زنده.
17. **برنامه‌هایی مثل Visual C++ دیگر «Managed by vendor» نیستند** — همان
    کاری که دستور `winget upgrade` در CMD می‌کند، حالا برنامه هم انجام می‌دهد:
    نسخه‌ی جدید **Microsoft Visual C++ Redistributable** (تمام سال‌های ۲۰۰۵ تا
    ۲۰۲۲، هر دو معماری x86 و x64)، **Microsoft Edge WebView2 Runtime** و
    **.NET Runtime / Windows Desktop Runtime** مستقیماً از مخزن رسمی
    مایکروسافت (winget-pkgs — همان منبعی که خود winget استفاده می‌کند) چک
    می‌شود و لینک نصاب هم لینک رسمی سرورهای دانلود مایکروسافت است. اگر نسخه
    جدیدی وجود داشته باشد، ردیف «Update available» می‌شود و مثل بقیه برنامه‌ها
    می‌توانید دانلودش کنید. نکته‌ی مهم درباره‌ی دقت: اگر نسخه‌ی نصب‌شده با
    جدیدترین نسخه‌ی رسمی یکی باشد (حتی با نوشتاری متفاوت مثل «12.0.40664» و
    «12.0.40664.0»)، ردیف صادقانه «Up to date» نشان داده می‌شود و اگر نسخه‌ی
    نصب‌شده از نسخه‌ی رسمی جدیدتر باشد (مثل بعضی ردیف‌های VC++ که آپدیت امنیتی
    گرفته‌اند)، هرگز پیشنهاد دانلود قدیمی‌تر داده نمی‌شود. برنامه‌هایی که واقعاً
    خودشان آپدیت می‌شوند (Steam، Spotify، درایورها و…) همچنان «Managed by
    vendor» نشان داده می‌شوند، چون آپدیتشان واقعاً با خود سازنده است.
18. **کاهش حداکثری «Managed by vendor» با تکنیک UniGetUI** — UniGetUI برای
    پیدا کردن آپدیت برنامه‌ها، خودِ دستور winget را می‌پرسد که هر برنامه‌ی
    نصب‌شده به کدام بسته‌ی رسمی کاتالوگ winget تعلق دارد. ما همین تکنیک را
    الگوبرداری کردیم: یک بار در روز برنامه `winget list` را اجرا می‌کند
    (فقط خواندنی — هیچ نصبی از این راه انجام نمی‌شود)، جدول خروجی‌اش را با
    روش ستون‌محورِ خود UniGetUI تجزیه می‌کند (روی هر زبانی از ویندوز کار
    می‌کند)، و نتیجه را ۲۴ ساعت کش می‌کند. از آن به بعد هر برنامه‌ای که
    winget بشناسد — حتی اگر در لیست دستی ما نباشد — از مخزن رسمی
    winget-pkgs چک می‌شود و اگر آپدیت داشته باشد مثل بقیه قابل دانلود است.
    اگر winget نصب نباشد یا برنامه‌ای را نشناسد، همان توضیح صادق قبلی
    (Managed by vendor / No source found) می‌آید و هیچ چیز خراب نمی‌شود.
19. **تأیید اصالت فایل با SHA-256** — وقتی منبع رسمی چک‌سام منتشر کند
    (مانیفست‌های winget و فایل‌های GitHub Release که digest دارند)، بعد از
    پایان دانلود، فایل با هش رسمی مقایسه می‌شود. اگر مطابقت نداشت، فایل
    حذف می‌شود و هرگز به پوشه دانلود راه پیدا نمی‌کند. پیام موفقیت هم
    «SHA-256 verified» نشان می‌دهد. پنجره‌ی Details هم می‌گوید آیا منبع
    چک‌سام منتشر کرده یا نه.
20. **آیکون کنار ساعت (System Tray) + چک خودکار زمان‌بندی‌شده + اعلان** —
    آیکون برنامه کنار ساعت می‌نشیند (دابل‌کلیک = باز شدن؛ راست‌کلیک =
    Open/Check now/Quit). در تنظیمات می‌توانید انتخاب کنید چک آپدیت هر چند
    ساعت خودکار انجام شود (هرگز/۲/۴/۶/۱۲/۲۴ ساعت). بعد از چکِ خودکار (نه
    دستی)، اگر آپدیت جدید باشد یک اعلان ویندوز با تعداد آپدیت‌ها نشان داده
    می‌شود که از تنظیمات قابل خاموش کردن است. ToolTip آیکون هم تعداد
    آپدیت‌های در انتظار را نشان می‌دهد.
21. **پنجره‌ی جزئیات هر برنامه** — راست‌کلیک روی هر ردیف → «View details»:
    ناشر، نسخه نصب‌شده، نسخه جدید، وضعیت، منبع، حجم دانلود، معماری، وضعیت
    SHA-256، توضیح کامل چکر و **لینک مستقیم نصاب** با دکمه کپی و دکمه باز
    کردن صفحه رسمی.
22. **رد کردن یک نسخه‌ی خاص (Skip version)** — اگر نسخه‌ی جدید برنامه‌ای
    را نخواهید (مثلاً چون باگ دارد)، راست‌کلیک → «Skip this version» — آن
    نسخه دیگر آپدیت نشان داده نمی‌شود، اما نسخه‌های جدیدتر همان برنامه
    عادی پیشنهاد می‌شوند. ردیف چنین برنامه‌ای «Version skipped» می‌شود و
    از منوی کلیک‌راست قابل برگشت است. در تنظیمات (بخش Applications) هم
    دکمه «Restore skipped versions» همه را یکجا برمی‌گرداند.
23. **پشتیبان‌گیری لیست برنامه‌ها (Export/Import)** — در تنظیمات بخش
    «Backup & Maintenance»: دکمه «Export app list» کل لیست برنامه‌ها با
    نسخه‌ها و وضعیت‌شان را در یک فایل JSON ذخیره می‌کند؛ دکمه «Import app
    list» همان فایل را با نصب‌های فعلی این کامپیوتر مقایسه می‌کند و گزارش
    می‌دهد چه برنامه‌هایی اینجا نصب نیستند، کدام‌ها هنوز آپدیت لازم دارند و
    کدام‌ها به‌روزند. فایل کاملاً محلی است — هیچ چیز جایی آپلود نمی‌شود.
24. **فایل لاگ + ثبت کرش‌ها** — برنامه حالا یک لاگ چرخشی کوچک (حداکثر
    ۵۱۲KB×۳) در `%LOCALAPPDATA%\SoftUpdater\logs\softupdater.log` نگه
    می‌دارد: شروع برنامه، نتایج چک‌ها، دانلودها، تأیید/رد هش‌ها و همه‌ی
    خطاها. اگر خطای غیرمنتظره‌ای رخ دهد، جزئیات هم در لاگ و هم در
    `error.log` قدیمی ثبت و یک پیام واضح نمایش داده می‌شود — دیگر هیچ
    خطایی «بی‌صدا» نیست. دکمه «Open log folder» هم در تنظیمات هست.
25. **بازطراحی تنظیمات از حالت عمودی به افقی + رفع باگ به‌هم‌ریختگی متن** —
    قبلاً پنجره‌ی Settings همه‌ی بخش‌ها را در یک ستون باریک عمودی نشان
    می‌داد و اگر فونت را بزرگ می‌کردید، خودِ پنجره هم‌اندازه‌ی قبلی می‌ماند؛
    نتیجه‌اش روی‌هم‌افتادن متن‌ها، بریده‌شدن دکمه‌ها و ناخوانا شدن گزینه‌ها
    بود. حالا تنظیمات مثل برنامه‌های حرفه‌ای چیده شده: یک نوار منوی باریک
    در سمت چپ (Checking / Downloads / Applications / Backup / Appearance)
    و هر بخش در صفحه‌ی خودش — یعنی پنجره همیشه کوتاه و جادار است. مهم‌تر
    اینکه پنجره با هر تغییر فونت **خودش را اندازه می‌کند** (از ۹ تا
    ۱۸ پوینت هیچ متنی بریده یا روی هم نمی‌رود)، آدرس پوشه‌ی دانلود اگر بلند
    باشد وسطش «...» می‌گذارد و مسیر کامل در ToolTip هست، و دکمه‌های
    Backup دوستونه‌اند تا در هیچ سایزی از کادر بیرون نزنند.
26. **رفع کامل دانلودهای «تقلبی» (مثل VLC) + دانلود هرگز ناقص نمی‌ماند** —
    دانلودر حالا بعد از هر انتقال، تعداد بایت‌ها را با Content-Length
    سرور مقایسه می‌کند؛ اگر وسط راه اینترنت قطع شود (که در ایران خیلی
    رایج است)، خودش تا دو بار دیگر تلاش می‌کند — اول از همان‌جا که قطع
    شده ادامه می‌دهد (Resume با Range) و بعد از صفر. فایل‌های ناقص،
    صفحه‌های خطا و فایل‌های غیر واقعی (فاقد امضای PE ویندوز یا خیلی
    کوچک) قبل از اینکه به پوشه‌ی دانلود برسند **پاک می‌شوند** و پیام
    واضح می‌بینید؛ فایل سالم بعد از ذخیره‌ی نهایی هم یک بار دیگر تأیید
    می‌شود. اگر به هر دلیلی ویندوز نتوانست نصاب را باز کند، خودش پوشه‌ی
    دانلود را باز می‌کند و مسیر فایل را نشان می‌دهد.
27. **رفع باگ «Check failed»های پرتکرار و ناپایدار** — ریشه‌ی مشکل این
    بود که برنامه برای فهمیدن آخرین نسخه‌ها از GitHub API استفاده می‌کرد
    که فقط ~۶۰ درخواست در ساعت می‌دهد (و روی IPهای اشتراکی ایران زودتر
    تمام می‌شود)؛ به‌محض تمام شدن سهمیه، بقیه‌ی برنامه‌ها «Check failed»
    می‌گرفتند. حالا مثل UniGetUI مسیر اصلی اصلاً از API نیست: تغییر
    مسیر `releases/latest` خودِ سایت گیت‌هاب + فید `releases.atom` +
    صفحه‌ی `expanded_assets` (که حتی SHA-256 و حجم هر فایل را هم می‌دهد)
    — همه بدون سهمیه. GitHub API فقط آخرین پشتیبان است. علاوه بر این،
    هر درخواست در صورت قطعی موقت شبکه تا ۳ بار با فاصله‌ی زمانی مناسب
    تکرار می‌شود و اگر یک بررسی هم شکست بخورد، **آخرین نتیجه‌ی موفق
    قبلی** ردیف پاک نمی‌شود (کنارش نوشته می‌شود «showing the last known
    result»).
28. **پوشش برنامه‌های «Managed by vendor» از طریق کاتالوگ winget** —
    ترتیب منابع هوشمند شد: کاتالوگ داخلی → تطبیق winget با برنامه‌های
    نصب‌شده → جستجوی کاتالوگ winget (برای برنامه‌هایی که در لیست winget
    جا نیفتاده‌اند) → جستجوی GitHub فقط به‌عنوان آخرین گزینه. یعنی
    برنامه‌هایی مثل Canva، LDPlayer، BlueStacks X، HAXM و ده‌ها برنامه‌ی
    دیگر حالا از مانیفست رسمی winget نسخه‌ی جدید + لینک نصاب + SHA-256
    می‌گیرند. حتی اگر منبع GitHub یک برنامه نصاب نداشته باشد، قبل از
    اعلام «بدون نصاب» یک بار از winget نجات داده می‌شود. ضمناً ۱۹
    برنامه‌ی محبوب (OBS، 7-Zip، ShareX، KeePassXC، DBeaver، Anki،
    MuseScore، ImageGlass و…) که اسم فایل‌های نصابشان عوض شده بود
    اصلاح شدند و دوباره آپدیت‌هایشان پیدا می‌شود.
29. **آیکون واقعی برای همه‌ی برنامه‌ها** — خیلی از برنامه‌ها (Java، LAV
    Filters، Discord، Epic Games Launcher، Google Chrome و…) به‌جای آیکون
    واقعی فقط یک دایره با حرف اول نشان می‌دادند، چون تنها منبع آیکون، مقدار
    DisplayIcon رجیستری بود و خیلی از نصاب‌ها اصلاً آن را ثبت نمی‌کنند یا
    مسیرش قدیمی شده است. حالا آیکون هر برنامه با یک زنجیره‌ی چندمرحله‌ای
    پیدا می‌شود: (۱) همان DisplayIcon رجیستری؛ (۲) **شورتکات منوی استارت و
    دسکتاپ** — فایل .lnk از نظر باینری باز می‌شود و آیکونی که خود شورتکات
    نشان می‌دهد یا فایلی که به آن اشاره می‌کند استخراج می‌شود (دقیقاً همان
    چیزی که شما در منوی استارت می‌بینید)؛ (۳) پوشه‌ی نصب برنامه بهترین
    فایل exe هم‌نام را پیدا می‌کند (فایل‌های unins/setup نادیده گرفته
    می‌شوند)؛ (۴) پوشه‌های معروف (Program Files / LocalAppData) برای پوشه‌ای
    با نام همان برنامه می‌گردند. اگر هنوز چیزی پیدا نشد (مثل LAV Filters که
    اصلاً exe ندارد)، مثل UniGetUI از **بانک آیکون‌های جامعه‌ی UniGetUI**
    استفاده می‌شود که یک بار دانلود و چند روز کش می‌شود و تصویرش از نظر
    امضای فایل تأیید می‌شود. همه‌ی آیکون‌ها روی دیسک کش می‌شوند (با آپدیت
    برنامه، آیکون نو می‌شود) و اگر هیچ‌کدام نشد، همان کاشی حرف اول تمیز
    قبلی می‌ماند. اگر بخواهید برنامه کاملاً بدون اینترنت بماند، متغیر محیطی
    `SOFTUPDATER_OFFLINE_ICONS=1` آیکون‌های اینترنتی را خاموش می‌کند.
30. **رفع ریشه‌ای «دانلود شد ولی نصاب باز نشد» (VLC و همه‌ی برنامه‌های دیگر)** —
    مشکل این بود که برای هر برنامه فقط **یک** آدرس دانلود وجود داشت؛ اگر
    همان یک آینه/سرور خراب بود یا فیلترینگ وسط راه فایل را قطع می‌کرد یا
    به‌جای فایل صفحه‌ی خطا می‌داد، دانلود شکست می‌خورد و طبعاً نصابی برای
    باز شدن نبود. حالا برنامه برای هر فایل می‌تواند چند **سرور رسمیِ جایگزین**
    داشته باشد و به‌ترتیب امتحانشان می‌کند — مثلاً برای VLC علاوه بر
    get.videolan.org (انتخابگر آینه‌ی جغرافیایی)، سرور رسمی خود VideoLAN
    یعنی download.videolan.org هم هست؛ اگر اولی خراب/مسدود بود، دومی خودکار
    ادامه می‌دهد. صفحه‌های خطا/فیلترینگ (محتوای HTML به‌جای فایل) دیگر حتی
    دانلود شروع نمی‌شوند (سریع رد می‌شوند) و اگر با ظاهر باینری بیایند،
    امضای PE و SHA-256 رسمی آن‌ها را رد می‌کند — یعنی «فایل تقلبی» عملاً
    غیرممکن است. فایل چک‌سام رسمی هم حالا فقط وقتی پذیرفته می‌شود که شکل
    یک چک‌سام واقعی داشته باشد (باگ واقعی: یک صفحه‌ی HTML با کد 200 می‌توانست
    هش اشتباه بدهد و فایل سالم را حذف کند — پیدا و رفع شد). اگر همه‌ی
    سرورهای رسمی شکست بخورند، پیام وضعیت دقیقاً می‌گوید چیزی ذخیره نشده و
    راهنمایی می‌کند به «Open official page». دکمه‌های Cancel/Pause هم روی
    اینترنت‌های کند حالا فوری (<۱ ثانیه) پاسخ می‌دهند و اگر یک بررسی مجدد
    شکست بخورد، لینک و هش دانلود قبلی هم مثل نسخه و منبع حفظ می‌شود.

31. **بررسی مجدد خودکار بعد از نصب (دیگر نیازی به راست‌کلیک نیست)** — تا
    حالا بعد از دانلود و نصب آپدیت (مثلاً VLC)، باید خودتان روی ردیف
    راست‌کلیک می‌کردید و «Re-check this app» را می‌زدید تا برنامه بفهمد
    آپدیت شده و ردیف به «Up to date» تغییر کند. حالا این کار **خودکار**
    است: به محض اینکه دانلود یک نصاب با موفقیت تمام شد، برنامه در پس‌زمینه
    (بدون هیچ درخواست اینترنتی) هر ~۱۲ ثانیه رجیستری ویندوز را نگاه
    می‌کند؛ همان لحظه که نسخه‌ی نصب‌شده تغییر کرد (یعنی نصب را تمام
    کرده‌اید)، خودش همان یک برنامه را بررسی مجدد می‌کند، ردیف بلافاصله به
    «Up to date» سبز می‌رود، در نوار وضعیت پیام تأیید می‌آید
    («VLC media player updated to ... - now up to date ✓») و اگر اعلان‌ها
    روشن باشد یک Toast هم می‌بینید. این نظارت تا یک ساعت بعد از دانلود
    فعال است (اگر دیرتر نصب کنید، همان راست‌کلیک قدیمی هم همچنان کار
    می‌کند)، تشخیص نسخه با همان منطق مطمئنِ مطابقت رجیستری انجام می‌شود
    (برنامه‌های هم‌نام مثل VC++ های مختلف هیچ‌وقت با هم قاطی نمی‌شوند) و
    اگر بعد از بررسی، نسخه‌ی نصب‌شده هنوز از آخرین نسخه عقب باشد، ردیف
    صادقانه همان «Update available» می‌ماند. برای دانلودهای ناموفق هیچ
    نظارتی فعال نمی‌شود.

32. **نصاب رسمی (Setup EXE) برای انتشار عمومی** — فایل جدید
    `build_installer.bat` و پوشه‌ی `installer` اضافه شد. با یک
    دابل‌کلیک، علاوه بر خودِ برنامه، یک نصاب رسمی ویندوز هم ساخته
    می‌شود («SoftUpdater-Setup-1.0.0.exe»): ویزارد نصب با صفحه‌ی
    مجوز، نصب در Program Files، شورتکات منوی استارت و دسکتاپ،
    برنامه‌ی Uninstall در Apps & features و آپگرید خودکار نسخه‌های
    بعدی روی نسخه‌ی قبلی (بدون ساختن برنامه‌ی دوبله). متادیتای رسمی
    (نام/نسخه/کپی‌رایت) داخل exe قرار می‌گیرد و SHA-256 فایل نهایی
    برای صفحه‌ی انتشار چاپ می‌شود. شماره‌ی نسخه در فایل `VERSION`
    نگه‌داری می‌شود؛ برای هر انتشار جدید فقط همان را عوض کنید.
    توضیح گام‌به‌گام در بخش «ساخت فایل نصبی واقعی» پایینتر.

33. **تفکیک واضح دو بات + قالب آماده‌ی امضای نصاب** — `build.bat`
    فقط نسخه‌ی پرتابل (پوشه) می‌سازد و بنر آن حالا همین را صریح
    می‌گوید؛ فایل نصبی واقعی فقط با `build_installer.bat` ساخته
    می‌شود. در `installer/SoftUpdater.iss` هم قالب آماده‌ی
    `SignTool` گذاشته شد تا بعد از خرید گواهی Code Signing، نصاب
    مثل release امضاشده‌ی اندروید استودیو امضا شود (بخش
    «امضای نصاب» راهنما).

34. **خروجی تک‌فایل شد + ریشه‌یابی ارور «Failed to load Python
    DLL»** — تا حالا PyInstaller خروجی را به‌صورت یک پوشه می‌ساخت
    (`SoftUpdater.exe` + پوشه‌ی `_internal` کنارش)؛ اگر فقط exe را
    جابه‌جا می‌کردید، ویندوز همین ارور را می‌داد. حالا خروجی هر دو
    بات یک **تک‌فایل** `SoftUpdater.exe` خودکفاست که به پوشه‌ی کناری
    هیچ نیازی ندارد و هر جا ببرید کار می‌کند (اولین اجرا چند ثانیه
    بیشتر طول می‌کشد — طبیعی است). مسیر نصاب رسمی همچنان
    `dist\installer\SoftUpdater-Setup-1.0.0.exe` است و محتوای نصب‌شده
    هم حالا فقط همان یک فایل در پوشه‌ی نصب کاربر است.

35. **پوشه‌ی dist بعد از ساخت نصاب فقط همان Setup را نشان می‌دهد** —
    قبلاً هم‌زمان با نصاب، یک `SoftUpdater.exe` پرتابلی هم داخل dist
    بود و باعث می‌شد فایل تستی با نصاب اشتباه گرفته شود (کلیک روی آن
    بدون هیچ مرحله‌ی نصبی برنامه را باز می‌کرد). حالا در
    `build_installer.bat` فایل میانی برنامه به پوشه‌ی کاری
    `build\app` منتقل می‌شود و در پایان داخل `dist` فقط
    `installer\SoftUpdater-Setup-1.0.0.exe` وجود دارد — یعنی هر exe
    که بعد از build_installer.bat در dist ببینید، همان نصاب واقعی
    است که ویزارد نصب دارد.

36. **ساخت نصاب بدون هیچ پیش‌نیاز اضافه + نصب per-user بدون UAC** —
    گزارش واقعی کاربر نشان داد مرحله‌ی Inno Setup روی بعضی سیستم‌ها
    شکست می‌خورد و پوشه‌ی dist اصلاً ساخته نمی‌شد. یک نسخه‌ی میانی
    برای رفع آن به سراغ IExpress (ابزار ساخت نصابِ داخل خود ویندوز)
    رفت که نتیجه‌اش روی سیستم کاربر همان ارور «Error opening the
    IExpress Self Extraction Directive file» شد — IExpress ابزار
    خیلی قدیمی دهه‌ی ۹۰ ویندوز است و روی مسیرهای دارای نویسه‌های
    فارسی و بسیاری حالت‌های دیگر کلاً از کار می‌افتد. این مسیر به‌کلی
    حذف شد (فایل `installer/make_iexpress_files.py` هم پاک شد).
    حالا `build_installer.bat` اگر **Inno Setup 6** را نصب‌شده پیدا
    نکند، خودش آن را از انتشار رسمی Inno Setup دانلود و بی‌صدا نصب
    می‌کند و بعد نصاب واقعی ساخته می‌شود. نصاب، برنامه را به‌صورت
    **per-user** در `%LOCALAPPDATA%\Programs\SoftUpdater` نصب می‌کند:
    بدون پنجره‌ی UAC و بدون نیاز به ادمین (مثل نصب‌کننده‌های user-setup
    مدرن مثل VS Code)، با شورتکات منوی استارت و دسکتاپ و ورودی حذف در
    Apps & features. چون فایل میانی برنامه در `build\app` قایم می‌شود،
    در dist فقط همان Setup دیده می‌شود.

37. **ساخت نصاب ضدخطا: لاگ مرحله‌به‌مرحله + باز شدن خودکار لاگ روی
    خطا + دو باگ پنهان دیگر** — (۱) فایل `VERSION` انتهای خط CRLF
    داشت و `set /p` یک نویسه‌ی CR نامرئی به داخل متغیر نسخه می‌برد که
    بررسی‌های «فایل خروجی ساخته شد؟» را بی‌دلیل خراب می‌کرد؛ حالا
    نسخه با `for /f` خوانده می‌شود. (۲) خروجی همه‌ی مراحل بزرگ (pip،
    PyInstaller، دانلود و نصب Inno Setup، کامپایل Inno Setup) در
    فایل‌های `build\log-pip.txt`، `build\log-pyinstaller.txt`،
    `build\log-innosetup-download.txt`، `build\log-innosetup-install.txt`
    و `build\log-iscc.txt` ذخیره می‌شود و اگر مرحله‌ای شکست بخورد،
    همان فایل لاگ **خودبه‌خود در Notepad باز می‌شود** تا دیگر هیچ
    خطایی بی‌صدا و بی‌جواب نماند. (۳) دانلود Inno Setup اعتبارسنجی
    می‌شود (امضای MZ و حداقل حجم) تا فایل نیمه‌کاره‌ی ناشی از قطع
    اینترنت هرگز به‌عنوان نصاب اجرا نشود؛ و اگر همه‌ی نسخه‌ها شکست
    بخوردند، پیام خطا صفحه‌ی رسمی دانلود دستی را هم نشان می‌دهد.

38. **رفع ارور «List index out of bounds (2). TList range is 0..1»
    هنگام کامپایل نصاب** — لاگ کاربر نشان داد همه‌چیز درست جلو رفته
    بود (دانلود و نصب خودکار Inno Setup 6.7.3 موفق شده بود و کامپایلر
    اجرا شده بود) ولی کامپایل دقیقاً روی خط بخش [Run] فایل
    `installer\SoftUpdater.iss` قطع می‌شد: تابع ISPP به نام
    StringChange با دو پارامتر صدا زده شده بود در حالی که سه پارامتر
    لازم دارد. آن تابع اصلاً ضروری نبود (نام برنامه هیچ نویسه‌ی &
    ندارد) و به‌کلی حذف شد؛ حالا آن خط فقط نام برنامه را جای‌گذاری
    می‌کند. یعنی آخرین مانع سر راه برداشته شد — دوباره
    `build_installer.bat` را اجرا کنید؛ چون Inno Setup حالا نصب است،
    مراحل دانلود و نصب آن رد می‌شوند و ساخت سریع‌تر هم تمام می‌شود.

39. **نسخه‌ی دوم نصاب: سازگار با ویندوز ۷ و ویندوزهای ۳۲ بیتی
    (`build_installer32.bat`)** — ستون فقرات قبلی (پایتون 3.13 +
    PySide6/Qt6) فقط روی Windows 10/11 و ۶۴ بیتی کار می‌کند و فایل
    ۳۲ بیتی رسمی هم ندارد؛ برای سیستم‌های دارای ویندوز ۷ یا ویندوز
    ۳۲ بیتی یک خط ساخت موازی اضافه شد: `requirements-win7.txt`
    (پایتون 3.8 + PyQt5 5.15 + PyInstaller 5.13 — همه با ویل رسمی
    ۳۲ بیتی، وجود تک‌تک ویل‌ها روی PyPI تأیید شد)،
    `installer/SoftUpdater-win32.iss` (همان AppId — دو نصاب جای هم
    نصب می‌کنند)، لایه‌ی سازگاری `app/qt_compat.py` که همان کد را
    بدون هیچ تغییری روی هر دو موتور Qt اجرا می‌کند (کل رابط کاربری
    روی PyQt5 آفلاین تست و PASS شد)، افزودن `from __future__ import
    annotations` به ۶ ماژول (پایتون 3.8 بدون آن روی حاشیه‌نویسی‌های
    مدرن کرش می‌کرد) و بات جدید که خروجی
    `dist\installer\SoftUpdater-Setup-<نسخه>-win32.exe` را می‌سازد
    و خروجی ۶۴ بیتی را پاک نمی‌کند.

40. **رفع ارور «Icon input file ... build\app\assets\app.ico not found»
    در ساخت نسخه‌ی ویندوز ۷ + سه سخت‌سازی دیگر** — لاگ شما نشان می‌داد بیلد
    ۳۲ بیتی همه‌ی مراحل را درست رفته و دقیقاً در مرحله‌ی «Copying icon to
    EXE» متوقف شده است. علت با خواندن سورس همان نسخه‌ی PyInstaller (5.13.2)
    ثابت شد: PyInstaller مسیر نسبیِ `--icon` را نسبت به پوشه‌ی `--specpath`
    حل می‌کند، نه ریشه‌ی پروژه؛ چون بات ۳۲ بیتی spec را داخل `build\`
    می‌گذارد، آیکون را در `build\app\assets\app.ico` می‌جست که وجود ندارد.
    حالا آیکون و اسکریپت ورودی با مسیر مطلق داده می‌شوند و مشکل کلاً
    برداشته شد. سه بهبود دیگر هم در همین بات انجام شد:
    (۱) **باندل Universal CRT (x86) داخل خود فایل exe** — تا حتی یک
    ویندوز ۷ SP1 که هیچ آپدیتی نصب نکرده هم برنامه را اجرا کند (بوت‌لودر
    PyInstaller 5.13 استاتیک لینک شده و خودش UCRT نمی‌خواهد — از سورس ویل
    رسمی بررسی شد — پس UCRT داخل بسته کافی است). (۲) **گیت ۳۲ بیتی بودن
    خروجی** — بعد از ساخت، لاگ چک می‌شود که واقعاً بوت‌لودر
    Windows-32bit-intel به کار رفته باشد؛ در غیر این صورت ساخت با توضیح
    خطا و باز شدن لاگ متوقف می‌شود. (۳) نصاب win32 حالا حداقلِ ویندوز را
    صریحاً `Windows 7 SP1` اعلام می‌کند (MinVersion=6.1sp1).

41. **رفع پیام «The following applications are using files that need
    to be updated by Setup» هنگام نصب** — این پیام خطا نبود:
    پرامپت استاندارد «Restart Manager» ویندوز است که هر وقت
    `SoftUpdater.exe` موقع نصب در حال اجرا باشد (برنامه باز مانده
    باشد یا پروسه‌اش بعد از حذف نسخه‌ی قبلی زنده مانده باشد) ظاهر
    می‌شود و اجازه‌ی بستن برنامه‌ها را می‌پرسد. چون این دیالوگ شبیه
    ارور به نظر می‌رسید، کلاً حذف شد: هر دو نصاب (`SoftUpdater.iss`
    و `SoftUpdater-win32.iss`) حالا `CloseApplications=no` دارند و
    در بخش `[Code]` خودشان، قبل از دست زدن به فایل‌ها، نمونه‌ی در
    حال اجرای برنامه را بی‌صدا می‌بندند (اول بستن مودبانه با
    taskkill بدون /F، بعد بستن قهری با /F به‌عنوان پشتیبان؛ اگر
    برنامه در حال اجرا نباشد هر دو فراخوانی بی‌اثرند). همین منطق
    قبل از حذف هم اجرا می‌شود (InitializeUninstall). یعنی نصب و حذف
    دیگر هیچ سؤالی نمی‌پرسند و هرگز روی «برنامه در حال اجراست»
    نمی‌ایستند.

42. **چندزبانه شدن کامل برنامه + راهنمای اولین اجرا (نسخه 1.1.0 —
    آماده‌ی انتشار اوپن‌سورس در گیت‌هاب)** — دو قابلیت بزرگ:
    (۱) **۶ زبان کامل**: انگلیسی، فارسی، عربی، پرتغالی (برزیل)،
    فرانسه و آلمانی — پوشش ۱۰۰٪ رابط کاربری (۳۴۵ رشته در هر زبان:
    دکمه‌ها، tooltips، منوی راست‌کلیک، پیام‌ها، نوت‌های بررسی‌کننده،
    خطاهای دانلود، اعلان‌های تری، تنظیمات، حتی توضیح «مدیریت توسط
    سازنده» برای ۸۳ برنامه‌ی خودبه‌روزشونده). زبان از
    **تنظیمات ← ظاهر ← زبان برنامه** فوراً عوض می‌شود — بدون
    ری‌استارت، بدون از دست رفتن هیچ حالتی؛ همه‌ی پنجره‌ها همان لحظه
    بازترجمه می‌شوند و برای فارسی/عربی کل رابط **راست‌به‌چپ** می‌شود
    (جدول، نوار کنار تنظیمات، حتی رسم دستی ستون «آخرین نسخه» و نوار
    شیشه‌ای انتخاب ردیف آینه می‌شوند). موتور i18n (پوشه‌ی
    `app/i18n/`) هیچ وابستگی‌ای ندارد، کلید گمشده به انگلیسی برمی‌گردد
    و سلف‌تست، تطابق کامل کلیدها و placeholder های هر ۶ زبان را
    گارانتی می‌کند.
    (۲) **راهنمای سریع اولین اجرا**: ۴ صفحه‌ی شیک هم‌سبک با تم شیشه‌ای
    (خوش‌آمد ← قابلیت‌های راست‌کلیک ← دانلودها ← شخصی‌سازی) با آیکون‌های
    وکتوری رسم‌شده با QPainter و نقطه‌های صفحه. تیک
    **«دیگر نشان داده نشود» برای همیشه ذخیره می‌شود** (رجیستری،
    `behavior/quick_tour_done`) و فقط حذف کامل برنامه آن را پاک
    می‌کند — هر دو نصاب حالا موقع Uninstall کل کلید
    `HKCU\Software\SoftUpdater` را پاک می‌کنند تا نصب مجدد واقعاً
    تازه شروع شود و راهنما دوباره بیاید. دکمه‌ی «نمایش دوباره
    راهنمای سریع» هم در تنظیمات است. تست‌ها: رابط کاربری روی هر دو
    موتور Qt (PySide6/PyQt5) آفلاین اجرا و تعویض زبان زنده en→fa→de→en
    روی همه‌ی اجزا PASS شد؛ اسکرین‌شات‌های RTL بازبینی شد؛ کل پروژه
    با `ast` روی سازگاری پایتون ۳.۸ (خط ویندوز ۷) سنجیده شد؛ یک باگ
    واقعی ناسازگاری PyQt5 (`QTimer.singleShot` سه‌آرگومانی) در این
    راستی‌آزمایی پیدا و رفع شد.

43. **رفع باگ «نمایش کلیدها به‌جای متن» در نسخه‌ی نصب‌شده (نسخه 1.1.1)** —
    در نسخه‌ی 1.1.0 اگر برنامه را با نصاب اجرا می‌کردید، همه‌ی متن‌های
    برنامه (دکمه‌ها، ستون‌های جدول، تنظیمات و راهنمای اولین اجرا)
    به‌صورت کلیدِ خام مثل «ob.welcome.title» و «btn.refresh» دیده
    می‌شد. علت: موتور ترجمه، ماژول هر زبان را در زمان اجرا با
    importlib بارگذاری می‌کرد؛ از سورس درست کار می‌کند اما در exe ی
    یک‌فایلیِ PyInstaller این ماژول‌ها اصلاً داخل بسته نمی‌آیند
    (تحلیل ایستای PyInstaller فراخوانی پویا را نمی‌بیند)، در نتیجه
    هیچ دیکشنری ترجمه‌ای پیدا نمی‌شد و برنامه کلیدها را نشان می‌داد.
    راه‌حل: بارگذاری هر ۶ زبان به فرم **ایستا** تبدیل شد (import
    مستقیم در `app/i18n/__init__.py`) تا PyInstaller در هر دو خط ساخت
    (۶۴ بیتی و ۳۲ بیتی/ویندوز ۷) آن‌ها را بدون هیچ پرچم اضافه‌ای داخل
    exe بگذارد. اثبات: همین پروژه با PyInstaller واقعی به‌صورت onefile
    بیلد و اجرا شد — هر ۶ زبان و همه‌ی کلیدهایی که در اسکرین‌شات‌ها خام
    دیده می‌شدند، داخل exe فریزشده درست ترجمه شدند؛ همچنین ۳۲۴ نقطه‌ی
    `tr()` کل کد و همه‌ی کلیدهای پویا (`status.*`، `src.*`، ۷۷ کلید
    `blk.*`، ۱۰ کلید `vn.*`، `note.*`، `ob.*`، `set.*`) در هر ۶ زبان
    راستی‌آزمایی شدند. برای گرفتن نسخه‌ی درست کافی است دوباره
    `build_installer.bat` یا `build_installer32.bat` را اجرا کنید
    (خروجی: `SoftUpdater-Setup-1.1.1...`).

44. **تعویض زبان همه‌چیز را بازترجمه می‌کند + مرتب‌سازی خودکار بعد از
    بررسی (نسخه 1.1.2)** — دو بهبود دقیقاً بر اساس بازخورد:
    (۱) **باگ تعویض زبان برطرف شد:** قبلاً اگر بررسی را با یک زبان انجام
    می‌دادید و بعد زبان را عوض می‌کردید، متن‌های ستون وضعیت عوض می‌شدند
    ولی توضیح‌های زیرِ آن‌ها (مثل «منبع رسمی برای این برنامه پیدا نشد…»
    که با نگه‌داشن ماوس روی ستون وضعیت دیده می‌شود و توضیح داخل پنجره‌ی
    جزئیات) به زبان قبلی می‌ماندند. علت: توضیح‌ها در لحظه‌ی بررسی به
    صورت «متن ترجمه‌شده» ذخیره می‌شدند. حالا همه‌ی توضیح‌ها (بیش از ۴۵
    پیام بررسی‌کننده + ۷۷ دلیل «مدیریت توسط سازنده» + خطاهای غیرمنتظره)
    به‌صورت **کلید کانیکال** ذخیره و در لحظه‌ی نمایش به زبان جاری
    ترجمه می‌شوند — یعنی تعویض زبان، حتی وسط کار، همه‌ی ردیف‌ها،
    tooltip ها و پنجره‌ی جزئیات را همان لحظه بازترجمه می‌کند؛ مهم نیست
    بررسی با چه زبانی انجام شده باشد.
    (۲) **مرتب‌سازی خودکار بعد از بررسی ۱۰۰٪:** وقتی یک بررسی کامل تمام
    شد، جدول خودش مرتب می‌شود: اول برنامه‌های **آپدیت‌دار**، بعد
    **به‌روزها** (و «به‌روزرسانی با سازنده» و «نصاب برای این معماری
    نیست»)، بعد **«منبعی پیدا نشد»ها** و در آخر **خطاهای قرمز** — و
    داخل هر گروه، الفبای طبیعی نام برنامه‌ها از بالا به پایین (مثلاً
    App 9 قبل از App 10 می‌آید، نه بعدش). انتخاب شما هم حفظ می‌شود.
    مرتب‌سازی فقط بعد از بررسی کامل انجام می‌شود؛ re-check تکی یک
    برنامه ترتیب را به‌هم نمی‌زند. بعد از مرتب‌سازی خودکار، کلیک روی
    سرستون‌ها مرتب‌سازی نمی‌کند تا این ترتیب ثابت بماند.

45. **چهار رفع باگِ سایز فونت و تشخیص ویندوز (نسخه 1.1.3)** — دقیقاً
    بر اساس بازخورد جدید:
    (۱) **جهت اسلایدر سایز فونت در فارسی و عربی:** قبلاً در این دو زبان
    دسته‌ی اسلایدر آینه می‌شد، ولی «نوار سبزِ پرشدگی» همچنان از سمت چپ
    رشد می‌کرد؛ یعنی جهت اسلایدر واقعاً عوض نمی‌شد (هرچه سایز کمتر،
    نوار پُرتر دیده می‌شد). حالا در فارسی/عربی کمینه‌ی اسلایدر سمت
    **راست** است، کشیدن به سمت **چپ** سایز را بیشتر می‌کند و پرشدگی هم
    از **راست** به چپ رشد می‌کند — دقیقاً برعکسِ زبان‌های دیگر، مثل هر
    برنامه‌ی راست‌به‌چپ استاندارد. درست بودن آن با تست پیکسلی خودکار
    (تحلیل تصویر واقعی اسلایدر) در موتور Qt5 تأیید شده و چون فقط از
    QSS و جهت استاندارد Qt استفاده می‌کند، در بیلد ۶۴ بیتی (Qt6) هم
    همین رفتار را دارد.
    (۲) **تشخیص ویندوز ۱۱ به‌جای ۱۰:** قبلاً بالای برنامه روی ویندوز ۱۱
    نوشته می‌شد «Windows 10»! علت: نام پایه از رجیستری (ProductName)
    گرفته می‌شد که مایکروسافت در ویندوز ۱۱ هنوز آن را «Windows 10 …»
    می‌گذارد. حالا نام پایه **فقط از شماره‌ی بیلد** تشخیص داده می‌شود
    (بیلد ۲۲۰۰۰ به بالا = ویندوز ۱۱) و رجیستری فقط برای پسوندِ نسخه
    (Pro / Home / …) به کار می‌رود؛ بنابراین دیگر هیچ‌وقت ویندوز ۱۱ به‌جای
    ۱۰ نمایش داده نمی‌شود. ویندوزهای سرور و نسخه‌های قدیمی‌تر (۸.۱، ۷،
    Vista) هم درست تشخیص داده می‌شوند و خواندن رجیستری در بیلد ۳۲ بیتی
    روی ویندوز ۶۴ بیتی از نمای ۶۴ بیتی انجام می‌شود.
    (۳) **لرزش صفحه هنگام تغییر سایز فونت:** قبلاً هر تیکِ حرکت اسلایدر
    یا فلش‌ها، کل برنامه را همان لحظه دوباره‌چیدمان می‌کرد و صفحه
    می‌لرزید؛ ناچار بودید دونه‌دونه سایز را کم و زیاد کنید. حالا
    پیش‌نمایش زنده با وقفه‌ی کوتاه (۰٫۲ ثانیه پس از ثابت‌شدن عدد)
    اعمال می‌شود: با یک حرکت پیوسته به سایز دلخواه می‌رسید و فقط یک
    تغییرِ نرم روی صفحه می‌آید. عددِ سایز همان لحظهٔ حرکت به‌روز
    می‌شود، دکمه‌ی Done سایز نهایی را قطعی اعمال می‌کند و Cancel هم
    مثل قبل دقیقاً به سایز قبلی برمی‌گردد.
    (۴) **فلش‌های کادر عدد سایز فونت** که تقریباً نامرئی بودند، حالا
    **سفید و کاملاً واضح** رندر می‌شوند (خالص QSS بدون فایل تصویر؛ در
    هر دو بیلد ۶۴ و ۳۲ بیتی یکسان دیده می‌شوند).

46. **حذف کامل لرزش + کادر پیش‌نمایش فونت + فلش‌های مثلثی + بازهٔ ۱۴ تا ۲۰
    (نسخه 1.1.4)** — بر اساس بازخورد بعدی:
    (۱) **لرزش صفحه (رفع قطعی):** حتی نسخهٔ قبل هم بعد از ثابت‌شدن عدد،
    یک تغییر روی کل برنامه می‌انداخت. حالا هنگام کشیدن اسلایدر یا زدن
    فلش‌ها **هیچ‌چیز روی برنامه اعمال نمی‌شود**؛ به‌جایش در صفحهٔ
    «ظاهر» یک کادر **«پیش‌نمایش»** زیر اسلایدر هست که متن نمونه را
    دقیقاً با سایز انتخاب‌شده نشان می‌دهد (ارتفاع کادر ثابت است، پس
    حتی خود پنجرهٔ تنظیمات هم در طول حرکت ذره‌ای تکان نمی‌خورد). سایز
    انتخابی فقط یک‌جا و فقط وقتی روی کل برنامه اعمال می‌شود که دکمهٔ
    **«تمام / Done»** را بزنید.
    (۲) **فلش‌های کادر عدد:** در نسخهٔ قبل به‌جای فلش، دو **مربع سفید**
    دیده می‌شد — QSS واقعاً نمی‌تواند مثلث بکشد! حالا فلش‌ها با
    QPainter به شکل **مثلث‌های سفیدِ صاف و واضح** رسم می‌شوند
    (بالا=بزرگ‌تر، پایین=کوچک‌تر)؛ در حالت راست‌به‌چپ هم جای‌شان درست
    می‌ماند و در هر دو بیلد ۶۴ و ۳۲ بیتی یکسان دیده می‌شوند.
    (۳) **بازهٔ سایز فونت:** کمینه **۱۴** و بیشینه **۲۰** pt شد و
    پیش‌فرض هم ۱۴ است؛ اگر قبلاً عددی کمتر ذخیره کرده بودید، در اجرای
    بعدی خودکار به ۱۴ می‌رسد. متن راهنمای همین صفحه هم به‌روز شد و به
    شش زبان ترجمه شده است.


47. **آیکون‌های برداری شیک در همه‌ی بخش‌های برنامه (نسخه 1.1.5)** — الهام
    گرفته از ظاهر UniGetUI:
    (۱) **موتور آیکون اختصاصی:** یک مجموعه‌ی ۲۷تایی آیکون برداری به سبک
    «Fluent» (مثل آیکون‌های خود ویندوز ۱۱) با QPainter رسم می‌شود — بدون
    فونت آیکون، بدون فایل تصویر، بدون هیچ کتابخانه‌ی جدید؛ بنابراین در
    هر دو بیلد ۶۴ بیتی و وین۷/۳۲ بیتی **دقیقاً یکسان** رندر می‌شود و در
    هر DPI و هر سایز فونت (۱۴ تا ۲۰) شارپ می‌ماند.
    (۲) **کجا آیکون اضافه شد:** لوگوی کنار عنوان برنامه؛ دکمه‌های
    Refresh / Open folder / Settings؛ سه دکمه‌ی اصلی (Check برای همه با
    آیکون سپر، Check selected، Download با آیکون سبز)؛ ذره‌بین داخل کادر
    جست‌وجو؛ Pause/Resume و Cancel در پنل دانلود (آیکون Pause هنگام توقف
    به Play تبدیل می‌شود)؛ **منوی راست‌کلیک هر برنامه** (جزئیات، بررسی
    مجدد، دانلود، صفحه‌ی رسمی، رد کردن نسخه، پنهان کردن)؛ منوی System
    Tray؛ دکمه‌های دیالوگ جزئیات (کپی/صفحه‌ی رسمی/بستن)؛ **نوار کناری
    تنظیمات** (هر بخش آیکون خودش را دارد و آیکون بخش فعال سبز می‌شود)؛
    همه‌ی دکمه‌های تنظیمات (بازنشانی، مرور، خروجی/ورودی، گزارش، تور،
    تمام)؛ و دکمه‌های قبلی/بعدی تور شروع (فلش‌ها در فارسی و عربی به سمت
    درست آینه می‌شوند).
    (۳) **نقطه‌ی رنگی وضعیت:** پایین پنجره کنار متن وضعیت یک دات کوچک
    هست که با وضعیت رنگ عوض می‌کند — خاکستری (عادی)، سبز (موفق/در حال
    دانلود)، کهربایی (در حال بررسی/هشدار)، قرمز (خطا).
    (۴) **RTL و زبان‌ها:** آیکون‌های جهت‌دار (فلش تور، برگرداندن) در
    فارسی/عربی آینه می‌شوند؛ بقیه آیکون‌ها جهت‌بی‌طرف‌اند و جایشان در
    چیدمان راست‌به‌چپ خودکار درست است. هیچ متن جدیدی اضافه نشد، پس هر
    شش زبان بدون تغییر می‌مانند. پنجره‌ی اصلی هنگام باز شدن کمی عریض‌تر
    شده (۱۲۴۰ به‌جای ۱۰۸۰) تا ردیف دکمه‌ها با آیکون‌هایش راحت جا شود.

48. **تراز عمودی دقیق آیکون‌ها با متن دکمه‌ها (نسخه 1.1.6)** — همان‌طور
    که گفتید آیکون‌ها «یه ذره بالاتر» از متن دکمه قرار می‌گرفتند. علت
    فنی: کیوت آیکون را با مرکزِ جعبه‌ی فونت (بلوک ارتفاع صعود و نزول
    حروف) وسط‌چین می‌کند، ولی چشم آدمی متن را در «نوار حروف» می‌بیند که
    در فونت Segoe UI ویندوز حدود ۱ تا ۱٫۵ پیکسل پایین‌تر از آن مرکز
    است؛ برای همین آیکونِ از نظر هندسی وسط‌چین، کمی بالاتر از متن دیده
    می‌شد. راه‌حل: موتور آیکون حالا هر نقش را بر اساس متریک‌های واقعی
    فونتِ همان لحظه (ascent / capHeight / descent) دقیقاً به همان اندازه
    پایین می‌کشد و آیکون عموداً هم‌سطح متن می‌نشیند:
    (۱) کاملاً خودکار است — برای هر فونت، هر سایز (۱۴ تا ۲۰)، هر زبان و
    هر DPI اندازه‌ی درست خودش را محاسبه می‌کند؛
    (۲) در منوی راست‌کلیک، منوی System Tray، ذره‌بین جست‌وجو، نقطه‌ی
    وضعیت و همه‌ی دکمه‌ها اعمال می‌شود؛
    (۳) نقاشی لوگوی کنار عنوان (کاشی تمام‌قاب) مستثناست و سر جایش
    می‌ماند؛
    (۴) هیچ متن یا تمی عوض نشد و هر دو بیلد ۶۴ بیتی و وین۷/۳۲ بیتی
    دقیقاً یکسان رندر می‌کنند.

49. **آماده‌سازی اپن‌سورس + صفحه‌ی «درباره» با امضای توسعه‌دهنده (نسخه 1.1.7)** —
    برای انتشار عمومی در گیت‌هاب:
    (۱) **بخش جدید «درباره» در تنظیمات:** نسخه‌ی برنامه، امضای
    توسعه‌دهنده (**BuiltByAmirali**)، دکمه‌ی «کد رسمی در گیت‌هاب» (باز
    می‌کند مخزن رسمی را)، یادداشت مجوز MIT و آدرس رسمی مخزن — تا همیشه
    بتوان نسخه‌ی رسمی را از هر کپی دیگری تشخیص داد (جای لینک حمایت
    مالی هم بعداً همین‌جا امن است)؛
    (۲) **امضا در همه‌جا:** فایل LICENSE (مجوز MIT به نام
    BuiltByAmirali)، صفحه‌ی مجوز نصب‌کننده، متادیتای نسخه‌ی exe
    (Company/Copyright در Properties ویندوز)، ناشر هر دو نصب‌کننده و
    سربرگ سورس‌ها — مقادیر قدیمیِ موقتی («SoftUpdater Project» و آدرس
    اشتباه github.com/softupdater) کامل حذف شدند؛
    (۳) **مستندات گیت‌هاب:** README انگلیسی با دکمه‌ی نسخه‌ی فارسی
    (README.fa.md)، CHANGELOG.md (تغییرات همه‌ی نسخه‌ها)،
    CONTRIBUTING.md، SECURITY.md و .gitignore؛
    (۴) **ابزار checksum:** اسکریپت build\make_checksums.bat بعد از
    ساخت نصب‌کننده‌ها فایل SHA256SUMS.txt می‌سازد تا کاربران بتوانند
    سلامت دانلود ۳۲ و ۶۴ بیتی را بررسی کنند؛
    (۵) هیچ تغییری در عملکرد اصلی — فقط بخش About اضافه شد و همه‌ی
    تست‌ها (۳۰ تست آیکون، خودآزمایی، ۶ زبان) دوباره پاس شدند.

## اجرای سریع (بدون ساخت exe)

1. پایتون ۳.۱۰ یا جدیدتر را از python.org نصب کنید (هنگام نصب تیک
   «Add python.exe to PATH» را بزنید) — فقط یک بار لازم است.
2. روی `run.bat` دابل‌کلیک کنید. اولین بار محیط مجازی می‌سازد و کتابخانه‌ها را
   نصب می‌کند (به اینترنت نیاز دارد)، بعد برنامه باز می‌شود.

## ساخت فایل نصبی واقعی (Setup EXE) — مسیر اصلی انتشار

**پاسخ مستقیم به «با چه نرم‌افزاری این کد را به exe نصابی تبدیل کنم؟»:
Inno Setup 6** — نرم‌افزار رایگان و استاندارد ساخت نصاب ویندوز
(jrsoftware.org). و هیچ کار دستی هم لازم نیست؛ همه‌چیز از قبل در
پروژه خودکار شده است.

دو فایل بات در پروژه داریم و تفاوت‌شان مهم است:

| فایل | خروجی | کاربرد |
|---|---|---|
| `build.bat` | **تک‌فایل پرتابل** `dist\SoftUpdater.exe` (بدون نصاب) | تست سریع روی سیستم خودتان |
| **`build_installer.bat`** | **تک‌فایل `SoftUpdater-Setup-1.0.0.exe`** | **انتشار برای دانلود — همان چیزی که می‌خواهید** |

**چرا آن ارور «Failed to load Python DLL» را دیدی؟** چون خروجی قدیمی
PyInstaller یک پوشه بود (`SoftUpdater.exe` + پوشه‌ی `_internal` کنارش)
و وقتی فقط exe را به دسکتاپ می‌بری، ویندوز نمی‌تواند پایتون را پیدا
کند. این مشکل **کلاً از بین رفت**: از حالا خروجی PyInstaller یک
**تک‌فایل** خودکفاست و به هیچ پوشه‌ی کناری نیاز ندارد — می‌توانی آن
را هر جا ببری (حتی دسکتاپ) و اجرا کنی. فقط اولین اجرا چند ثانیه
بیشتر طول می‌کشد؛ طبیعتِ حالت تک‌فایل است و ایراد نیست.

**چرا آن ارور «IExpress Wizard - Error opening the IExpress Self
Extraction Directive file» را دیدی؟** چون نسخه‌ی قبلی بات، وقتی
Inno Setup را پیدا نمی‌کرد، به سراغ IExpress می‌رفت — ابزارِ ساخت
نصابِ بسیار قدیمی که داخل خود ویندوز هست. IExpress با مسیرهای دارای
نویسه‌ی فارسی (مثلاً نام کاربری فارسیِ ویندوز) و خیلی حالت‌های دیگر
اصلاً از کار می‌افتد و همان ارور بی‌مصرف را می‌دهد؛ یعنی هرگز به
`dist\installer\SoftUpdater-Setup-1.0.0.exe` نمی‌رسید. در این نسخه
IExpress به‌کلی حذف شده و جای آن **Inno Setup 6 خودکار دانلود و نصب
می‌شود** — همان ابزار استانداردی که نرم‌افزارهای واقعی استفاده
می‌کنند. اگر ویندوز وسط نصبِ آن پرسید اجازه می‌خواهد، فقط Yes را
بزنید.

**دقت مهم:** آن تک‌فایل `dist\SoftUpdater.exe` که با `build.bat`
ساخته می‌شود **نصاب نیست و نیست که باشد** — مثل برنامه‌های پرتابل،
با دابل‌کلیک مستقیم باز می‌شود و هیچ مرحله‌ی نصبی ندارد. نصاب واقعی
خروجیِ `build_installer.bat` است؛ و از این نسخه به بعد، بعد از اجرای
آن، پوشه‌ی `dist` فقط و فقط یک چیز دارد:
`dist\installer\SoftUpdater-Setup-1.0.0.exe` (برنامه‌ی میانی در
پوشه‌ی کاری `build\app` نگه‌داری می‌شود تا با نصاب اشتباه نشود).
وقتی Setup را اجرا کنید می‌بینید که مثل بقیه‌ی اپ‌های نصابی، صفحه‌ی
مجوز، Next، انتخاب پوشه، نوار پیشرفت Install و در پایان تیک
«Launch SoftUpdater» می‌آید و برنامه در منوی استارت و Apps &
features می‌نشیند.

یعنی: برای گرفتن فایل نصبی که به هرکسی بدهید و او برنامه را مثل یک
نرم‌افزار واقعی نصب کند، **همیشه `build_installer.bat` را اجرا کنید،
نه `build.bat` را**.

مراحل (روی ویندوز خودتان):

1. پیش‌نیاز: فقط پایتون ۳.۱۰ یا جدیدتر (هنگام نصب تیک
   «Add python.exe to PATH»). **هیچ چیز دیگری لازم نیست** —
   `build_installer.bat` اگر **Inno Setup 6** را نصب‌شده پیدا کند از
   آن استفاده می‌کند وگرنه خودش آن را از انتشار رسمی Inno Setup
   دانلود و بی‌صدا نصب می‌کند (اگر ویندوز اجازه خواست، Yes بزنید).
   هر مرحله هم لاگ اختصاصی دارد (`build\log-*.txt`) و اگر مرحله‌ای
   خطا بدهد، لاگش خودکار در Notepad باز می‌شود.
2. روی **`build_installer.bat`** دابل‌کلیک کنید. این فایل به‌ترتیب:
   - شماره‌ی نسخه را از فایل `VERSION` می‌خواند (فعلاً 1.0.0)؛
   - با PyInstaller برنامه را می‌سازد و متادیتای رسمی (نام محصول،
     نسخه، کپی‌رایت) را داخل exe می‌گذارد — در Properties ویندوز
     دیده می‌شود؛
   - با Inno Setup 6 از خروجی برنامه، فایل
     `dist\installer\SoftUpdater-Setup-1.0.0.exe` را می‌سازد؛
   - در پایان پوشه‌ی `dist` فقط همین فایل Setup را دارد (فایل
     میانی برنامه در `build\app` است و به چشم نمی‌خورد)؛
   - در پایان **SHA-256** فایل را چاپ می‌کند.
3. فایل `SoftUpdater-Setup-1.0.0.exe` تک‌فایل نهایی برای انتشار است —
   فقط همین یک فایل را برای هرکسی بفرستید یا برای دانلود بگذارید.
   کاربر آن را اجرا می‌کند و ویزارد نصب واقعی باز می‌شود:
   صفحه‌ی مجوز (License) → Next → Install → Finish و در انتها تیک
   «Launch SoftUpdater». نصب **per-user** است: بدون UAC و بدون نیاز به
   دسترسی ادمین، برنامه در `%LOCALAPPDATA%\Programs\SoftUpdater`
   قرار می‌گیرد، شورتکات **منوی استارت** و **دسکتاپ** ساخته می‌شود
   و برای حذف، «SoftUpdater» در Apps & features ظاهر می‌شود.
4. انتشار نسخه‌های بعدی: فقط عدد داخل `VERSION` را عوض کنید (مثلاً
   1.0.1) و دوباره `build_installer.bat` را اجرا کنید. چون شناسه‌ی
   برنامه (AppId) ثابت است، کاربر با نصب نسخه‌ی جدید، نسخه‌ی قبلی
   را **آپگرید** می‌کند — نه یک برنامه‌ی جدا.

## امضای نصاب — معادل «Release امضاشده» اندروید استودیو

مقایسه‌ی درست همین است: فایل `SoftUpdater-Setup-1.0.0.exe` مثل
APK/release اندروید است؛ و آنچه در اندروید «امضای release» نام دارد،
در ویندوز **گواهی Code Signing** است. وضعیت‌ها:

- **بدون امضا (همین حالا):** نصاب کاملاً کار می‌کند و برنامه‌ی
  سالمی است؛ فقط ویندوز هشدار SmartScreen
  («Windows protected your PC») می‌دهد. کاربر «More info» →
  «Run anyway» می‌زند و نصب عادی ادامه پیدا می‌کند. برای شروع و
  توزیع بین کاربرها کافی است.
- **با امضا (حرفه‌ای):** هشدار حذف می‌شود و فایل رسمی و امضاشده
  حساب می‌شود. مراحل:
  1. یک گواهی Code Signing بگیرید — گزینه‌های رایج:
     Azure Trusted Signing (ماهی ~۱۰ دلار)، Certum Open Source
     Code (ارزان، مناسب پروژه‌های متن‌باز)، SSL.com یا Sectigo؛
     بنیاد SignPath هم برای پروژه‌های متن‌بازِ فعال گواهی رایگان
     می‌دهد.
  2. گواهی را در ویندوز نصب کنید، در Inno Setup از منوی Tools گزینه‌ی
     **Configure Sign Tools** را با نام `signtool` بسازید.
  3. در فایل `installer\SoftUpdater.iss` خط `;SignTool=signtool $p`
     را از کامنت خارج کنید (قالب آماده‌اش داخل همان فایل هست) و
     دوباره `build_installer.bat` را اجرا کنید — خروجی، نصابِ
     امضاشده است.

## انتشار فایل برای دانلود (پیشنهاد اصولی)

- بهترین میزبانی رایگان و رسمی، صفحه‌ی **Releases یک مخزن گیت‌هاب**
  است: یک repo بسازید، در بخش Releases یک Release با تگ `v1.0.0`
  بسازید و دو چیز را ضمیمه کنید: فایل `SoftUpdater-Setup-1.0.0.exe`
  و مقدار SHA-256 که بات چاپ کرد (در توضیحات Release). کاربرها از
  همان‌جا دانلود می‌کنند و می‌توانند صحت فایل را بررسی کنند.
- جایگزین‌ها: گوگل‌درایو/دایرکت لینک هاست شخصی — فقط لینک دانلود
  مستقیم فایل را بدهید و SHA-256 را کنارش بنویسید.

## نسخه‌ی سازگار با ویندوز ۷ و ویندوزهای ۳۲ بیتی (Setup -win32)

از این نسخه **دو فایل نصب رسمی** داریم و این جدول دقیقاً می‌گوید هر
کدام کجا کار می‌کند:

| فایل نصب | ویندوزهای پشتیبانی‌شده |
|---|---|
| `SoftUpdater-Setup-1.0.0.exe` | Windows 10 و 11 — فقط ۶۴ بیتی |
| `SoftUpdater-Setup-1.0.0-win32.exe` | Windows 7 SP1 / 8 / 8.1 / 10 / 11 — هر دو ۳۲ و ۶۴ بیتی |

اگر مطمئن نیستید گیرنده ویندوزش چیست، همان فایل `-win32` را بدهید —
روی همه‌ی ویندوزهای بالا اجرا می‌شود.

چرا دو فایل؟ چون پایتون 3.13 و Qt6 (ستون فقرات نسخه‌ی اصلی) فقط روی
Windows 10 به بالا و فقط ۶۴ بیتی کار می‌کنند و اصلاً فایل ۳۲ بیتی رسمی
ندارند. نسخه‌ی win32 روی **پایتون 3.8 + PyQt5 5.15** ساخته می‌شود
(آخرین پایتون و آخرین کیوت با پشتیبانی ویندوز ۷ و ۳۲ بیتی) — همان
برنامه با همان رابط کاربری، اما روی ویندوزهای قدیمی هم اجرا می‌شود.
کتابخانه‌های سیستمی لازم — از جمله MSVC Runtime و **Universal CRT** —
داخل خود فایل بسته می‌شوند؛ یعنی حتی یک ویندوز ۷ SP1 که هیچ آپدیتی
نصب نکرده هم برنامه را اجرا می‌کند. فقط ویندوز ۷ باید **SP1** باشد.

مراحل ساخت (روی ویندوز خودتان — یک‌بار):

1. پایتون 3.8 نسخه ۳۲ بیتی را یک‌بار نصب کنید (کنار پایتون فعلی‌تان
   می‌نشیند و هیچ چیزی را خراب نمی‌کند):
   `https://www.python.org/ftp/python/3.8.10/python-3.8.10.exe`
   (گزینه‌های پیش‌فرض را نگه دارید.)
2. روی **`build_installer32.bat`** دابل‌کلیک کنید. خروجی:
   `dist\installer\SoftUpdater-Setup-1.0.0-win32.exe`
3. برای انتشار **هر دو فایل**: اول `build_installer.bat` را اجرا کنید
   بعد `build_installer32.bat` را (بات اول پوشه‌ی dist را خالی
   می‌کند؛ بات دوم فقط فایل خودش را اضافه می‌کند و خروجی قبلی را
   پاک نمی‌کند).

نکته‌ها:

- هر دو نصاب **AppId یکسان** دارند: اگر هر دو روی یک سیستم نصب شوند
  یکی جای دیگری را می‌گیرد (دو برنامه‌ی جدا دیده نمی‌شود).
- نصاب win32 هم مثل نسخه‌ی اصلی per-user است: بدون UAC، با شورتکات
  استارت/دسکتاپ و حذف از Apps & features.
- مسیر ویندوز ۷ به‌صورت آفلاین قابل تست است: با متغیر
  `SOFTUPDATER_QT5=1` کل رابط کاربری روی PyQt5 اجرا می‌شود — همین
  تست در این نسخه انجام و PASS شد (همان مسیر کدی که داخل
  SoftUpdater-Setup-win32.exe بسته می‌شود).
- هر دو نصاب دیگر سؤال «برنامه‌های در حال اجرا بسته شوند؟» را نشان
  نمی‌دهند: اگر SoftUpdater موقع نصب یا حذف باز باشد، خودِ نصاب
  بی‌صدا آن را می‌بندد و کار ادامه پیدا می‌کند (همان دیالوگ
  «applications are using files» که شبیه ارور به نظر می‌رسید).

## نسخه‌ی پرتابل (برای تست — نصاب نیست)

1. روی `build.bat` دابل‌کلیک کنید.
2. چند دقیقه صبر کنید تا PyInstaller کارش تمام شود.
3. خروجی، **تک‌فایل** `dist\SoftUpdater.exe` است — یک فایل خودکفا
   که به هیچ پوشه‌ی کناری نیاز ندارد؛ هر جا ببریدش اجرا می‌شود.
   این حالت فقط برای تست سریع است — برای انتشار، از
   `build_installer.bat` (بخش اول همین راهنما) استفاده کنید.

## استفاده از برنامه

- دکمه **Check for updates**: همه برنامه‌های نصب‌شده را از منابع رسمی بررسی می‌کند.
- دکمه **Check selected**: فقط ردیف‌های انتخاب‌شده را بررسی می‌کند.
- دکمه **Download selected installers**: نصابِ نسخه جدید را (متناسب با
  ۳۲ یا ۶۴ بیتی بودن ویندوز شما) در پوشه `Downloads\SoftUpdater` ذخیره می‌کند.
  **هیچ فایلی خودکار اجرا نمی‌شود**؛ نصب با خودتان است.
- دکمه **Open download folder**: پوشه دانلود را باز می‌کند.
- دکمه **Settings**: پنجره‌ی تنظیمات با نوار منوی کنار صفحه و پنج بخش —
  Checking (بررسی خودکار موقع باز شدن، فاصله‌ی چک خودکار، اعلان)،
  Downloads (پوشه‌ی دانلود)، Applications (برنامه‌های پنهان و نسخه‌های
  رد‌شده)، Backup (خروجی/ورودی لیست برنامه‌ها + پوشه‌ی لاگ) و Appearance
  (زبان برنامه با ۶ زبان زنده، اندازه فونت کل برنامه، ۹ تا ۱۸ پوینت،
  و دکمه‌ی «نمایش دوباره راهنمای سریع»).
  تغییر فونت فوراً پیش‌نمایش می‌شود و پنجره‌ی تنظیمات خودش را با آن هم‌اندازه
  می‌کند (در هیچ سایزی متن‌ها روی هم نمی‌روند)؛ **Done** ذخیره می‌کند و اگر
  پنجره را ببندید (ضربدر)، اندازه قبلی برمی‌گردد. **Reset font to default**
  هم اندازه پیش‌فرض (۱۱ پوینت) را برمی‌گرداند.
- دابل‌کلیک روی هر ردیف: صفحه منبع رسمی آن برنامه را در مرورگر باز می‌کند.
- کلیک راست روی هر ردیف: منوی میان‌بر با این گزینه‌ها — «View details»
  (پنجره جزئیات با لینک مستقیم نصاب)، «Re-check this app»
  (بررسی مجدد فقط همین برنامه)، «Download installer»، «Open official page»،
  «Skip/Un-skip version» (فقط وقتی آپدیت/نسخه رد‌شده‌ای وجود دارد)
  و «Hide this app» (پنهان کردن برنامه از لیست).
- آیکون کنار ساعت (System Tray): دابل‌کلیک = باز شدن برنامه؛ راست‌کلیک =
  منوی Open / Check for updates now / Quit. بعد از چک خودکار، تعداد
  آپدیت‌های پیدا شده به شکل اعلان ویندوز هم اعلام می‌شود (قابل خاموش شدن
  در تنظیمات).

## معنی وضعیت‌ها

| Status | معنی |
|---|---|
| Update available | نسخه جدید موجب است — می‌توانید دانلود کنید |
| Up to date | برنامه به‌روز است |
| Managed by vendor | آپدیت این برنامه با خودِ سازنده است (آپدیتر داخلی یا Windows Update) — ماوس نگه دارید تا توضیح بخوانید. توجه: اگر winget این برنامه را بشناسد دیگر این وضعیت را ندارد و مثل بقیه چک می‌شود (VC++ و WebView2 و .NET همیشه از مانیفست رسمی چک می‌شوند) |
| No installer for this OS | نسخه جدید هست ولی نصابِ سازگار با معماری ویندوز شما (۳۲/۶۴ بیتی) منتشر نشده |
| No source found | منبع رسمی برای این برنامه پیدا نشد |
| Check failed | خطای شبکه/سرور — دوباره تلاش کنید |
| Version skipped | این نسخه را خودتان با «Skip this version» رد کرده‌اید — نسخه‌های جدیدتر عادی پیشنهاد می‌شوند |

## نکات

- برای تست سلامت برنامه بدون اجرای رابط گرافیکی: `python run.py --selftest`
- اگر GitHub محدودیت تعداد درخواست (۶۰ درخواست در ساعت بدون حساب) را بخورد،
  برنامه صادقانه پیام «GitHub API rate limit reached» می‌دهد؛ کمی بعد دوباره
  امتحان کنید.
- در صورت بروز خطای غیرمنتظره، جزئیات در فایل لاگ چرخشی
  `%LOCALAPPDATA%\SoftUpdater\logs\softupdater.log` و هم‌چنین
  `%LOCALAPPDATA%\SoftUpdater\error.log` ذخیره می‌شود؛ دکمه «Open log
  folder» در تنظیمات هم همین پوشه را باز می‌کند.
